<% if !state || state.not_ready? %>
    <div class=" mb-5">
        <%= render partial: "users/load_data"%>
    </div>
<% elsif state.loading? %>
    <div class="d-flex justify-content-center mb-5">
        <div class="text-center">
            <h6>Veriler Yükleniyor...</h6>
            <div class="spinner-border text-dark" role="status">
                <span class="sr-only"></span>
            </div>
        </div>
    </div>
<% end %>

<%= render partial: "users/user_search_box" %>
<div id="users">
    <%= render partial: "users/users", locals: { users: users } %>
</div>
<div class="fixed-bottom d-flex justify-content-center mb-5">
    <button class="btn btn-dark rounded-circle" data-bs-toggle="modal" data-bs-target="#user_create_modal" style="width:50px;height:50px;">
        <i class="bi bi-person-fill-add"></i>
    </button>
</div>
<%= render partial: "users/user_create_modal" %>